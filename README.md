# 艺术与科技交叉日报

2026-04-30 今日艺术与科技交叉热点日报静态页面。

## GitHub Pages 部署

1. 将仓库推送到 GitHub。
2. 打开仓库 `Settings` → `Pages`。
3. `Build and deployment` 选择 `Deploy from a branch`。
4. 分支选择 `main`，目录选择 `/root`。
5. 保存后等待 Pages 发布完成。

本项目不需要构建步骤，入口文件是 `index.html`。
